---
description: macOS Apple Silicon port — project context and conventions for all work on this fork
alwaysApply: true
---

# Project: SimpleGraphic macOS arm64 Port

This fork's sole purpose is porting the SimpleGraphic C++ engine to native macOS Apple Silicon (arm64). All macOS work is on the `macos-port` branch.

## What NOT to touch
- Do not modify Windows-only code paths. Add `if(APPLE)` / `#ifdef __APPLE__` blocks alongside them — never replace or delete Windows code.
- Do not modify anything in `src/` (that is the PoB Lua layer in a different repo — it needs zero changes).
- Do not commit to `master` — that branch stays clean for upstream rebasing.

## Architecture
SimpleGraphic is a shared library (.dylib on macOS) that hosts LuaJIT and provides a GLFW window + ANGLE-based 2D renderer. The Lua game logic (~99% of PoB) is in a separate repo and requires no macOS changes.

## Key decisions already made
- **LuaJIT**: v2.1 beta via vcpkg overlay `luajit` port — **JIT disabled in PoB host** (`jit.off()` in `ScriptInit`) until launch stable; standalone `luajit` still JIT-capable. Do not switch Lua runtime without filing an issue first.
- **Graphics**: ANGLE with Metal backend. Do not switch to native OpenGL unless ANGLE proves unworkable (document in issue #5 if so).
- **Deployment target**: macOS 13.0. Do not lower this.
- **vcpkg triplet**: `arm64-osx` in `triplets/arm64-osx.cmake` (libc++ `-isystem` fix for Apple Clang).
- **ANGLE**: `angle` dependency must include the **`metal` feature** in `vcpkg.json`.
- **Build paths**: no spaces in the repo path (LuaJIT `make` breaks). Use `~/PoB-SimpleGraphic-build` worktree or relocate clone for local builds.

## Task tracking
Every task has a GitHub Issue. Before starting work, check [open issues](https://github.com/braggpd/PathOfBuilding-SimpleGraphic/issues?q=label%3Amacos-port+is%3Aopen) and assign yourself. Branch naming: `macos/issue-N-short-description`.

Current phase: **Phase 2** — runtime integration on `macos-port`. **Phase 1 #1–#5 complete**;
**#6 engine work merged**. **#8 in progress:** `Launch_oninit_only.lua` passes; **segfault on
`PLoadModule("Modules/Main")` → `Modules/Common.lua`**. Follow `MACOS_PORT.md` → **Next session — close #8**
(bisect Common.lua `require`s; `cmake --install` to PoB `runtime-macos`). Tip: `origin/macos-port` @ `26dc2b1`.
Spec: `docs/macos/issue-6-runtime-layout.md`.

**macOS engine fixes already on branch:** Lua `LoadModule`/`PLoadModule` (no nested C `lua_call` under `OnInit`);
`package.path`/`package.cpath`; null guards (`IsActive`, `KeyEvent`, `glfwWindowShouldClose`); full-path `luaL_loadfile`;
`jit.off()` in host. Do not use `print()` in bisect scripts — use `ConPrintf`.

## macOS build command
```bash
cmake -B build -S . \
  -DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake \
  -DVCPKG_TARGET_TRIPLET=arm64-osx \
  -DCMAKE_OSX_ARCHITECTURES=arm64
cmake --build build --config Release
cmake --install build --prefix ~/PoB-PoE2-build/runtime-macos
```

PoB dev launch (from PoB repo root): `ln -sfn "$(pwd)/runtime/SimpleGraphic" runtime-macos/SimpleGraphic` then
`./runtime-macos/"Path of Building-PoE2" ./src/Launch.lua`. Staged: `Launch_oninit_only.lua` (OK), `Launch_oninit_pload.lua` (Common load).

## LuaJIT smoke test (after vcpkg install)
```bash
export DYLD_LIBRARY_PATH="$(pwd)/vcpkg/installed/arm64-osx/lib"
./vcpkg/installed/arm64-osx/tools/luajit/luajit -e 'print("ok", jit and jit.arch)'
```
Standalone `luajit` needs `DYLD_LIBRARY_PATH` (no `LC_RPATH` on the tool); linked SimpleGraphic dylib does not.

## Platform files
- `engine/system/win/sys_macos.mm` — macOS Obj-C++ implementations
- `engine/system/win/sys_main.cpp` — platform abstraction (has `#ifdef __APPLE__` guards)
- `engine/system/win/sys_video.cpp` — GLFW window/context (mostly cross-platform)
- `win/entry.cpp` — Windows `RunLuaFileAsWin` export
- `mac/entry.cpp` — macOS `RunLuaFileAsWin` + `main()` (CMake selects via `APPLE`)

## PR conventions
- Title: `[macOS] short description (closes #N)`
- Always update the **Notes & Decisions Log** in `MACOS_PORT.md` for non-obvious choices
- Close the issue and tick the checkbox in `MACOS_PORT.md` when a task is done
